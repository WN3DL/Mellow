# Compiling and Building
- This project required Java 21 to be build/compiled
    - On macOS this can be achieved by running `export JAVA_HOME=$(/usr/libexec/java_home -v 21)` first before any `gradlew` commands
